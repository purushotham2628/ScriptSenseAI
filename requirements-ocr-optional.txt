# Optional research OCR dependencies.
# The backend runs without these packages, but enables additional engines/features
# when they are installed and their external model files/binaries are available.

# Multi-engine OCR
paddleocr>=2.7.0
pytesseract>=0.3.10

# OCR correction and language detection
symspellpy>=6.7.7
langdetect>=1.0.9
fasttext-wheel>=0.9.2

# Real-ESRGAN super-resolution
realesrgan>=0.3.0
basicsr>=1.4.2
facexlib>=0.3.0
gfpgan>=1.3.8
